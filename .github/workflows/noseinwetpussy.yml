name: noseinwetpussy
on:
  workflow_dispatch: {}
  push:
    branches:
      - sn
jobs:
  get_data:
    runs-on: ubuntu-latest
    steps:
      - name: Repo Visualizer
        uses: githubocto/repo-visualizer@0.8.2
